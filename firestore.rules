rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 1. Litigation Cases
    match /cases/{caseId} {
      allow read, write: if true;
    }

    // 2. Advocate User Profiles
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}

